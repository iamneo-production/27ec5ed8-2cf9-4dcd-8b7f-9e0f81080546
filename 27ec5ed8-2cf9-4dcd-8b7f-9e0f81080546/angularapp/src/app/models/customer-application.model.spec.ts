import { CustomerApplication } from './customer-application.model';

describe('CustomerApplication', () => {
  it('should create an instance', () => {
    expect(new CustomerApplication()).toBeTruthy();
  });
});
